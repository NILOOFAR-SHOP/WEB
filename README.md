# اسکرپر خودکار محصولات لیمون

این پروژه با استفاده از **GitHub Actions** به‌صورت خودکار و روزانه، اطلاعات محصولات برند لیمون را از سایت `limonware.com` استخراج و در فایل `data/products.json` ذخیره می‌کند.

## 🚀 راه‌اندازی سریع

### 1. کلون کردن مخزن
```bash
git clone https://github.com/your-username/nilofor-scraper.git
cd nilofor-scraper
